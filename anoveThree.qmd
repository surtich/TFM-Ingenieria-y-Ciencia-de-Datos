{{< include _setup.qmd >}}

# Análisis de la varianza con modelos de efectos mixtos.

```{r}
#| fig-cap: Respuesta a la pregunta 18 por usuario y tratamiento
df <- df_clean %>% mutate(Response = as.numeric(Response))

df %>%
    filter(Question == "Q18") %>%
    ggplot(aes(x = Treat, y = Response, group = Subject)) +
    geom_line(aes(color = Subject)) +
    theme_bw() +
    theme(legend.position = "none")
```
